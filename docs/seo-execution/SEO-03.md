# SEO-03 — technical SEO and performance

Prepared September 18, 2026. Starting revision: `3bf03f6288a16b578e49cb1375254c7285a70855`. Repository fixes: `verified_local`; hosting correction: `waiting_for_access`. Starting production observations are [read-only audit evidence](technical-tracking-audit.md), not a claim that this candidate is live.

Implemented two-offer schema/metadata parity, consistent canonical commercial-page roles, ordinary-link reachability for every sitemap landing, content-hashed changed homepage CSS/JS plus its imported checkout module, query-preserving retired-AI redirect, and private preview restrictions. A controlled trace experiment identified GTM forcing unstyled layout before blocking CSS finished. Moving the unchanged bootstrap after the stylesheet links fixed the large initial shift in pricing; the same fix is applied to process, About, Resources and the three root guide templates. No tag/container setting was changed. The candidate has seven sitemap URLs; the retired AI URL and unsupported orphaned Tesla/FSD page were removed. Tesla now has visible review containment and noindex rather than new promotion links. All existing guide/metro release gates and nine protected legacy Texas city HTML hashes remain intact.

Production redirect checking found eleven passing cases and two apex-host failures: root and `/about.html` return 307 instead of 308, with queries preserved. The live deployment matches the starting revision; app-level permanent rules already exist. Headers suggest an upstream domain redirect, but do not prove whether the effective setting belongs to Vercel or Cloudflare. Inspect the exact Barton domain setting, change the canonical redirect to 308 only in the separately authorized account scope, and rerun the full matrix. See [audit details and authoritative documentation](technical-tracking-audit.md).

Mobile/desktop homepage, pricing and article measurements, five-run methodology, initial-render caveats and final confirmation belong in [performance-evidence.md](performance-evidence.md). Field GSC/CrUX data is insufficient for both device categories; no field LCP, INP or CLS pass is claimed. Browser navigation timings are lab evidence, not organic impact or field INP.

The clean dependency install also found the existing development image dependency `sharp` below the patched version. It was updated to the compatible 0.35.4 patch with lockfile, followed by a clean install/audit and full checks. [The maintainer advisory](https://github.com/lovell/sharp/security/advisories/GHSA-rgj7-g3m4-5g8c) identifies the affected libheif decoding dependency; no unrelated dependency upgrade was made.

Final HTML/link/schema, redirect-config, generator, tests and browser results are recorded in [release-candidate.md](release-candidate.md). Next action: complete the candidate's connected gates, then verify the upstream redirect and live crawl behavior after authorized activation.
