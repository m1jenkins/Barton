# SEO-09 — integrated release candidate

Prepared September 18, 2026; follow-up September 19 UTC. Starting revision: `3bf03f6288a16b578e49cb1375254c7285a70855`. Latest tested revision: `acf9116bdcc48372340b94688f3476b9dc8a35b6`. Connected validation: partially complete, `waiting_for_access` for remaining application/destination boundaries, with accountable decisions also unresolved; local implementation including real PostgreSQL: `verified_local`. Production activation is not complete.

The integrating owner combined SEO-01/03/04/07 sequentially, with independent research/evidence from SEO-02/05/06/08. The exact candidate, changed public URLs, private exclusions, checks, review state, account/environment dependencies, cutover and rollback are in [release-candidate.md](release-candidate.md). [status.md](status.md) tracks each dependency and next action.

Unapproved SEO-05 editorial revisions live under `draft-artifacts/guides`; public source guides retain prior contained editorial bodies with only the authorized offer transition. The worksheet and hypothetical examples, city/metro drafts, source/review/outreach packets and private data remain deployment-excluded. No guide was made indexable, no fictional customer proof was created, and no protected artifact hash was changed.

The candidate is prepared for review and connected testing, not safe to activate with today's old Payment Links. Sandbox/preview database identity, lawful exact-total configuration, collector/destination reconciliation and unresolved accountable copy/terms review are recorded gates. The apex-host redirect correction is a separate concrete account operation. No deploy, push/merge, live Stripe write, GTM publication, sitemap submission or outbound communication occurred.

The [follow-up](SEO-09-connected-validation.md) and [sanitized Stripe evidence](stripe-test-evidence.json) record actual **test-mode $295/$895/$195/$495 payments**, current/legacy Stripe-hosted receipts, real isolated PostgreSQL migrations/concurrency, and reconciliation of those session snapshots through locally signed replay. The test AI link blocks new visits and previously open unpaid checkout after retirement while preserving the paid record/receipt. A $10 discount produces a $285 total; Austin 78701 tax calculation returns zero with `not_collecting`. Head-office ZIP 78704 differs from the owner's supplied 78701. MCP test access is available; actual Stripe-origin webhook delivery, SDK receipt retrieval, monitored email and external analytics acknowledgements remain unverified. Simple terms are drafted; tax/address, scope/refund and historical $100 credit decisions remain explicit.

Node `v24.20.0`: runner syntax, site/API/metro/city/buying checks, config-only redirects, diff checks, **160 regression tests (0 failed, 0 skipped)** and **eight PostgreSQL scenario groups** passed on the revision above. Dependencies were unchanged from the prior clean install. Test-only runner connection issues were fixed; no application defect was established. Changed files: `scripts/validate-connected-db.mjs`, `stripe-test-evidence.json`, the connected-validation packet, release candidate, cutover packet, this record and status tracker. Deployment files and real historical records are unchanged; test-only Stripe fixtures were created and historical test links retired.

Next action: supply the scoped application test key, matching signing secret/reachable preview webhook, approved collector/consent/destination and monitored test inbox; resolve exact scope/tax/address/credit decisions. Local browser permission for `http://localhost:8765` is pending after automatic approval review rejected it. Recreate the isolated database and finish the external gate boundaries; record actual evidence and fixes before separate activation of the exact revision. **Not ready for activation.** SEO-10 begins after actual activation.
