# Metro release execution — September 16, 2026

The current DFW, Houston, Austin and service-area hub files in `draft-artifacts/metros/` are private editorial drafts. Generate them with `npm run draft:metros`; verify them with `npm run check:metros`. The loopback preview is `npm run preview:metros`. The drafts are excluded from Vercel output by `.vercelignore`, and no new public metro URL, redirect, sitemap entry or indexing change is authorized. A `noindex` tag alone does not authorize publication.

## Current decision

[The metro release decision](metro-release-decision-2026-09-16.md) supersedes the earlier proposed Texas passages and review requests. The seven proposed passages were withdrawn before any evidenced exact-copy approval. They are absent from current dossiers, claim/source registries and rendered drafts. The earlier Tarrant inspection-checklist conflict is outside this rollout and does not block work on remote buying drafts. The original metro plan snapshots remain byte-preserved.

Drive Right helps buyers find and buy cars remotely. Buyers choose whether to buy and pay vehicle shipping charges. The agency does not attend visits, register or inspect vehicles, or perform safety work. Draft buyer scenarios are hypothetical. They make no claim about local prices, inventory, demand, seller relationships or transactions. Local evidence within this scope is insufficient to support the required two mapped local modules for any of the three markets. `data/metro-dossiers.json` therefore has empty `modules` arrays and `data/metro-release.json` keeps each market and the hub at `releaseStatus: draft`.

The historical Michael R., Sarah K. and David L. statements are reproduced exactly from the recovered historical site wording. The business owner attests to possession of the originals, permission to publish and records supporting the quoted outcomes. The private records stay outside the repository. Historical city labels are attribution context, not evidence of metro transactions or residency, and the quotes are not typical-result or extra-savings claims. Other content still follows `docs/claim-review-workflow.md`.

## Release contract

`scripts/metro-release.mjs`, invoked by `scripts/validate-site.mjs`, keeps the existing fail-closed gate. A market needs a concrete operating boundary and logistics, query/SERP and demand evidence, actual baseline, checkout parity, two claim/source-mapped local modules and a worksheet or permissioned case, current approved claim/source rows, editorial and qualified review, QA, service/entity/inventory parity, reviewed artifact hash, canonical and sitemap parity. The hub has separate review, QA and artifact approval. No field is set to approved merely because the owner confirmed availability or the testimonial attestation.

Google Search Console, GA4, qualified-lead, purchase and backlink baselines remain pending for a later session. Null baseline fields mean unknown, not zero. The owner authorized independent private draft work while access or exports are unavailable. The existing task `barton-metro-release-evidence-20260916` retains these obligations; do not create a duplicate task or claim launch eligibility.

The nine existing Texas HTML files, their current noindex behavior, existing discovery links and the sitemap remain under their previous containment and redirect review. Do not edit the original metro plan snapshots or use the old regulatory source collection as new publication evidence.
